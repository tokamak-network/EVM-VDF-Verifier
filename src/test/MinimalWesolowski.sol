// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

import "../libraries/WesolowskiLibrary.sol";

interface IMinimalWesolowski {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool);
}

contract MinimalWesolowski204820 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski204821 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski204822 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski204823 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski204824 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski204825 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307220 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307221 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307222 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307223 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307224 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}

contract MinimalWesolowski307225 {
    function verifyWesolowski(
        BigNumber memory x,
        BigNumber memory y,
        BigNumber memory n,
        BigNumber memory T,
        BigNumber memory pi,
        BigNumber memory l
    ) external view returns (bool) {
        WesolowskiLibrary.verify(x, y, n, T, pi, l);
        return true;
    }
}
